{
  "name": "wage-calculator",
  "private": true,
  "version": "1.0.0",
  "description": "wage-calculator",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "test.calc": "stencil test --spec --verbose",
    "generate": "stencil generate",
    "format": "prettier --write \"{src,tests}/**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@ionic/core": "^5.3.2",
    "@stencil/core": "^2.0.1",
    "@types/jest": "^26.0.20",
    "@types/puppeteer": "^5.4.2",
    "jest": "^26.6.3",
    "jest-cli": "^26.6.3",
    "prettier": "^2.5.1",
    "puppeteer": "^5.4.1"
  },
  "jest": {
    "preset": "@stencil/core/testing"
  }
}
